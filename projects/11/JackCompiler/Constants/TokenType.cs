namespace JackCompiler.Constants
{
    public enum TokenType
    {
        NotDefined,
        MultilineCommentStart,
        Keyword,
        String,
        Int,
        Identifier,
        Symbol
    }
}
